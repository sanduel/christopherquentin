#!/bin/bash -e

# Always ensure the database is migrated before the app boots. Robust against
# Railway's startCommand bypassing the older "if ends with rails server" check.
if [ "${RAILS_ENV}" = "production" ] || [ "${SKIP_DB_PREPARE}" != "1" ]; then
  ./bin/rails db:prepare
fi

exec "${@}"
