[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "./bin/rails db:prepare && ./bin/thrust ./bin/rails server"
healthcheckPath = "/up"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
