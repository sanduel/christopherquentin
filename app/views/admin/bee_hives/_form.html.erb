<%= form_with model: [:admin, record], local: true, multipart: true, class: "space-y-6 max-w-3xl" do |f| %>
  <% if record.errors.any? %>
    <div class="bg-red-50 border-l-4 border-red-400 p-4">
      <ul class="text-red-700 text-sm">
        <% record.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
    <div>
      <%= f.label :name, "Beekeeper name", class: "block text-sm font-medium text-stone-700 mb-1" %>
      <%= f.text_field :name, class: "w-full px-3 py-2 border border-stone-300 rounded" %>
    </div>
    <div>
      <%= f.label :email, class: "block text-sm font-medium text-stone-700 mb-1" %>
      <%= f.email_field :email, class: "w-full px-3 py-2 border border-stone-300 rounded" %>
    </div>
  </div>

  <div>
    <%= f.label :address, "Location (will re-geocode if changed)", class: "block text-sm font-medium text-stone-700 mb-1" %>
    <%= f.text_field :address, class: "w-full px-3 py-2 border border-stone-300 rounded" %>
  </div>

  <div>
    <%= f.label :story, "Story (optional)", class: "block text-sm font-medium text-stone-700 mb-1" %>
    <%= f.text_area :story, rows: 5, class: "w-full px-3 py-2 border border-stone-300 rounded" %>
  </div>

  <div>
    <%= f.label :photo, record.new_record? ? "Photo" : "Replace photo", class: "block text-sm font-medium text-stone-700 mb-1" %>
    <%= f.file_field :photo, accept: "image/*", class: "block" %>
    <% if record.photo.attached? %>
      <p class="text-xs text-stone-500 mt-1">Currently attached: <%= record.photo.filename %></p>
    <% end %>
  </div>

  <%= render "admin/shared/pin_overrides", f: f, record: record %>

  <div class="border-t border-stone-200 pt-6">
    <%= f.label :user_id, "Assign to user (optional)", class: "block text-sm font-medium text-stone-700 mb-1" %>
    <%= f.collection_select :user_id, User.order(:name), :id, :name,
          { include_blank: "— Anonymous (no account) —" },
          class: "w-full px-3 py-2 border border-stone-300 rounded" %>
    <p class="text-xs text-stone-500 mt-1">Leave blank to keep this bee hive attributed to its submitted name/email only.</p>
  </div>

  <div>
    <%= f.label :status, class: "block text-sm font-medium text-stone-700 mb-1" %>
    <%= f.select :status, BeeHive.statuses.keys.map { |k| [k.titleize, k] }, {}, class: "w-full px-3 py-2 border border-stone-300 rounded" %>
  </div>

  <div class="flex gap-3">
    <%= f.submit (record.new_record? ? "Create" : "Save"),
          class: "px-4 py-2 bg-stone-800 text-white rounded hover:bg-stone-700 transition cursor-pointer" %>
    <%= link_to "Cancel", admin_bee_hives_path,
          class: "px-4 py-2 border border-stone-300 text-stone-700 rounded hover:bg-stone-100 transition" %>
  </div>
<% end %>
