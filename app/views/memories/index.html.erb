<% content_for :title, "Timeline — Christopher Quentin McMullen-Laird" %>

<%# Header %>
<section class="max-w-[1180px] mx-auto px-6 lg:px-10 pt-20 pb-8">
  <div class="flex items-center gap-3">
    <span class="inline-block w-[34px] h-px bg-accent"></span>
    <span class="font-mono text-[12px] tracking-[0.2em] uppercase text-accent">Timeline · <%= pluralize(@memories_count, "memory", plural: "memories") %> · <%= pluralize(@contributors_count, "contributor") %></span>
  </div>
  <div class="mt-6 flex flex-col md:flex-row md:items-center md:justify-between gap-4">
    <p class="text-[16px] text-muted m-0">
      <%= pluralize(@memories_count, "memory", plural: "memories") %> · <%= pluralize(@contributors_count, "contributor") %>
    </p>
    <button type="button"
            data-controller="share-modal-trigger"
            data-action="share-modal-trigger#open"
            class="bg-ink text-white-bg no-underline text-[15px] font-semibold py-[15px] px-7 rounded-md hover:bg-ink-deep transition-colors cursor-pointer self-start md:self-auto">
      Share a memory
    </button>
  </div>
</section>

<%= render "memories/year_filter", years: @years, active_year: @active_year %>

<%# Timeline body %>
<section class="relative max-w-[1180px] mx-auto px-6 lg:px-10 py-8 min-h-[400px]">
  <%# Vertical spine — desktop only %>
  <div class="hidden lg:block absolute left-1/2 top-0 bottom-0 w-px bg-ink/10"
       aria-hidden="true"></div>

  <% if @timeline_by_year.any? %>
    <% @timeline_by_year.each do |year, items| %>
      <%= render "memories/year_marker", year: year, age: year - Memory::CHRIS_BIRTH_YEAR %>
      <% items.each do |item| %>
        <% if item.is_a?(Milestone) %>
          <%= render "memories/milestone", milestone: item %>
        <% else %>
          <%= render "memories/memory_card", memory: item %>
        <% end %>
      <% end %>
    <% end %>
  <% else %>
    <div class="text-center py-20">
      <p class="font-serif italic text-[22px] text-muted mb-4">No memories for <%= @active_year %>.</p>
      <%= link_to "Clear filter →", memories_path, class: "text-accent text-[15px] font-semibold no-underline hover:opacity-70 transition-opacity" %>
    </div>
  <% end %>
</section>
